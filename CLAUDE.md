# Nekter App — سياق المشروع لـ Claude Code

مشروع نظام كاشير/منيو ديجيتال لمحل مشروبات وقهوة اسمه **Nekter** (سلاش، موهيتو، عصائر طبيعية، آيس كريم، مشروبات ساخنة). ده ملف سياق مكتوب عشان أي جلسة Claude Code جديدة تفهم المشروع فورًا من غير شرح من الصفر.

## الستاك التقني
- **Frontend**: React 19 + Vite + React Router (كل واجهة من التلاتة بتتحمّل بـ `React.lazy` — code splitting بالـ route، شوف `src/App.jsx`)
- **Backend**: Supabase (Postgres + Auth + Realtime + Storage) — كان Firebase/Firestore وتم النقل بالكامل
- **مكتبات**: recharts (تقارير), xlsx (استيراد/تصدير Excel), @supabase/supabase-js
- **الخط**: Cairo بس في كل حتة (الأرقام والأسعار كمان) — مفيش IBM Plex Mono ولا أي خط تاني، مطابق لموقع nekterdrinksbar.sa الحقيقي
- **الاختبارات**: Vitest + React Testing Library — `npm test` لتشغيلها، ملفات `*.test.js(x)` جنب اللي بتختبره

## بنية التطبيق (3 واجهات، Route واحد لكل واحدة)
| المسار | الملف | الوظيفة |
|---|---|---|
| `/?table=N` | `src/pages/CustomerView.jsx` | منيو العميل + كارت + تتبع الأوردر + المكافآت + بحث + الأكثر طلبًا |
| `/cashier` | `src/pages/CashierView.jsx` | شاشة الكاشير (دخول بـ PIN بس — تاب "إدارة" اتشال) + تفعيل أكواد المكافآت + استرجاع/إلغاء طلب |
| `/admin` | `src/pages/AdminDashboard.jsx` | لوحة تحكم كاملة (دخول Supabase Auth حقيقي) |

## نظام التصميم المشترك
- `src/styles/theme.css` — كل الألوان/الخطوط/الأنيميشن كـ CSS variables وكلاسات (`n-card`, `n-btn`, `.stub`, `.tag-*`)
- **لون البراند**: تركواز واحد (`--teal` / `--teal-deep`) لكل حاجة، مطابق لموقع nekterdrinksbar.sa الحقيقي بالظبط (فحصناه مباشرة). العنبري (`--amber`) مخصص بس لسياق النقط/المكافآت، مش لتصنيف المنتجات
- `src/components/Icons.jsx` — مجموعة أيقونات SVG خطية موحّدة (نفس سمك الخط، نفس الستايل) تستخدمها كل الشاشات؛ **مفيش إيموجي كأيقونة وظيفية في التطبيق** (ما عدا نص إشعار المتصفح الأصلي، لأنه بيتحكم فيه نظام التشغيل مش الكود)
- `src/components/AnimatedNumber.jsx` — أرقام بتعد لفوق في لوحة التحكم
- `src/components/ConfettiBurst.jsx` — احتفال بصري عند إتمام الأوردر/الاستبدال

## طبقة البيانات (`src/supabase.js`)
- Firestore كان بيرجع snake_case من DB، الكود بيتوقع camelCase (زي أيام Firebase) — فيه Mapper تلقائي:
  - `mapOrderedRow(row)` / `unmapOrderedRow(obj)` — للجداول اللي فيها ترتيب (`products`, `categories`)، بيحول عمود `position` ↔ خاصية `order` في الكود
  - `rowToCamel` / `toSnakeRow` — تحويل عام camelCase ↔ snake_case لباقي الجداول

## قاعدة البيانات — الملفات لازم تتشغل بالترتيب ده في Supabase SQL Editor
1. `supabase/schema.sql` — الجداول الأساسية (`products`, `categories`, `orders`, `staff_users`) + RLS + RPCs للـ login الآمن + storage bucket
2. `supabase/auth-hardening.sql` — تقييد الكتابة على `products`/`categories` لمستخدم Supabase Auth فقط (بدل ما تكون مفتوحة لأي حد معاه الـ anon key)
3. `supabase/migration-2-new-features.sql` — يجمع 3 حاجات:
   - **صلاحيات إدارية متدرجة**: جدول `admin_profiles` (مدير عام / مشرف) + **سجل تدقيق تلقائي** (`audit_log`) بتريجرز على `products`/`categories`/`staff_users`
   - **كومبوهات وعروض**: عمودين إضافيين على `products` (`is_combo`, `combo_items`)
   - **نقاط قابلة للاستبدال**: جدولين `rewards` و `loyalty_redemptions`
4. `supabase/migration-3-harden-staff-auth.sql` — تشفير PIN/باسورد `staff_users` بـ pgcrypto (bcrypt)، وإلغاء تسجيل الدخول القديم بإيميل/باسورد (`rpc_login_manager`) خالص
5. `supabase/reset-transactional-data.sql` — سكريبت تشغيل يدوي (مش جزء من التسلسل) لتصفير الطلبات/الاستبدالات وقت الحاجة (مثلاً قبل إطلاق فعلي)، من غير ما يمس المنيو أو الحسابات

⚠️ لو عايز تعمل تعديل SQL جديد، اعمل ملف جديد بترقيم تسلسلي (`migration-4-...sql`) واحفظه في `supabase/` — متعدلش الملفات القديمة عشان تفضل سجل واضح لتاريخ التغييرات.

⚠️ ملحوظة تقنية مهمة: `pgcrypto` في Supabase غالبًا بينتصب في schema اسمه `extensions` مش `public` — أي دالة جديدة بتستخدم `crypt()`/`gen_salt()` لازم يكون فيها `set search_path = public, extensions` وإلا هتفشل بخطأ "function crypt does not exist".

## نظام "مدير" واحد موحّد (كان نقطتين منفصلتين، اتحل)
- **`admin_profiles`** (Supabase Auth الحقيقي) هو المصدر الوحيد لأي صلاحية مدير/مشرف — بيستخدمه `/admin` بس. تاب "إدارة" اللي كان جوه `/cashier` اتشال خالص (كان بيستخدم `staff_users` بإيميل وباسورد نص صريح، نسخة أضعف مكررة من نفس الفكرة).
- **`staff_users`** بقى دليل PIN للكاشير بس (`role = 'كاشير'`). الـ PIN متشفّر (bcrypt) ومبيترجعش للواجهة أبدًا حتى في شاشة "طاقم العمل" — تغييره بيبقى بإدخال رقم جديد (فاضي = سيبه زي ما هو)، مش عرض القديم.
- أي صفوف قديمة بـ `role = 'مدير'` في `staff_users` بقت بيانات تاريخية ميتة (مفيش أي login path بيوصلها بعد ما اتشال `rpc_login_manager`) — تسيبها أو تمسحها مش فارق وظيفيًا.

## الحالة الحالية (خلصان)
- ✅ الهجرة الكاملة من Firebase لـ Supabase (بيانات حقيقية منقولة)
- ✅ تصميم موحّد للثلاث واجهات مطابق للبراند الحقيقي + أيقونات SVG موحّدة (لا إيموجي ولا ticket-stub) + أنيميشن/تفاعلات
- ✅ تسجيل دخول حقيقي لـ `/admin` (Supabase Auth)، وتوحيد نظامي "المدير" في واحد
- ✅ صلاحيات متدرجة (مدير عام/مشرف) + سجل تدقيق تلقائي
- ✅ كومبوهات وعروض + قسم "الأكثر طلبًا" (قابل للتصفية كتصنيف كمان) + بحث في المنيو
- ✅ مفضّلة ومشاركة لكل منتج (رابط مباشر يفتح على المنتج ويدخل قسمه تلقائي)
- ✅ نقاط ولاء قابلة للاستبدال (كود يتفعّل من الكاشير)
- ✅ إشعار فوري للعميل لما طلبه يبقى جاهز (Notification + صوت + توست، من غير ريفريش)
- ✅ استرجاع/إلغاء طلب من شاشة الكاشير (مستبعد من المبيعات والنقاط، ظاهر بعلامة "ملغي" في سجل الفواتير للتدقيق)
- ✅ إمكانية مسح بيانات العميل السابق ("مش انت؟") قبل ما عميل جديد يطلب من نفس الطاولة
- ✅ تنبيه مخزون منخفض (banner في كل شاشات الأدمن + إشعار متصفح)
- ✅ سجل فواتير فيه بحث/فلترة/تصدير Excel
- ✅ Code splitting بالـ route (كل واجهة بتحمّل كودها بس)، وأول مجموعة اختبارات آلية (Vitest)
- ✅ إزالة `/upload-data` — كان route عام من غير تسجيل دخول بيقدر يمسح المنيو كله

## الباقي في الـ backlog
1. **الدفع الإلكتروني الحقيقي** — محتاج المستخدم يفتح حساب مع بوابة دفع (Moyasar/PayTabs) الأول
2. **إضافة مدير جديد من داخل `/admin` مباشرة** بدل الدخول لـ Supabase Dashboard يدويًا — محتاج Supabase Edge Function آمنة (السر `service_role` ميقدرشي يبقى في الفرونت إند أبدًا)، قرار معلق لسه
3. **تنبيهات مخزون خارج المتصفح** (واتساب/إيميل) — محتاج Supabase Edge Function + حساب خدمة رسائل
4. دعم أكتر من فرع، PWA، لغة إنجليزية — أفكار مستقبلية مش مؤكدة بعد

## حاجات لازم الحرص عليها
- كل الألوان لازم تتكتب بـ CSS variables (`var(--teal)` إلخ) مش hex مباشر، عشان الاتساق
- أي جدول جديد في Supabase لازم ياخد RLS policy واضحة (مفيش جدول من غير RLS)
- الـ anon key في `src/supabase.js` مش سر (زي أي public client config)، بس أي مفتاح `service_role` **متضافش للكود أو للـ git أبدًا**
