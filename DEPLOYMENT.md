# Nekter App — بريف النشر (Deployment)

**السياق الكامل للمشروع موجود في `CLAUDE.md` جنب الملف ده — اقرأه الأول.**

## القرار المتفق عليه
ننشر التطبيق أونلاين دلوقتي بأسرع وأبسط طريقة، ونأجل الباقي (توحيد نظامي المدير، الدفع الإلكتروني، تنبيهات واتساب) لمرحلة بعد النشر.

## نوع المشروع (مهم لاختيار الاستضافة)
- **React + Vite** — تطبيق front-end بالكامل (Static Site)، مفيش سيرفر خلفي مطلوب
- **Supabase** — كل البيانات والـ Auth بيتعملوا من المتصفح مباشرة (client-side)، مفيش API خاص لازم يتنشر منفصل
- ده معناه: أي استضافة static hosting (Vercel / Netlify / Cloudflare Pages) هتشتغل تمام، من غير احتياج لسيرفر Node.js شغال باستمرار

## الخطوات المطلوبة بالترتيب

### 1. اختيار الاستضافة
**التوصية: Vercel** (الأسهل مع مشاريع Vite/React، وربط مباشر مع GitHub، ومجاني للاستخدام ده).
البديل: Netlify (نفس الفكرة تقريبًا).

### 2. ربط الـ GitHub Repo
الريبو موجود بالفعل: `https://github.com/AbanoubHanna/Nekter-app`
- من Vercel Dashboard → Add New Project → Import Git Repository → اختار الريبو ده

### 3. إعدادات البناء (Build Settings)
| الإعداد | القيمة |
|---|---|
| Framework Preset | Vite |
| Build Command | `npm run build` |
| Output Directory | `dist` |
| Install Command | `npm install` |

### 4. ⚠️ نقطة حرجة: SPA Routing
التطبيق فيه 3 مسارات (`/`, `/cashier`, `/admin`) بيتعاملوا عن طريق React Router **من غير سيرفر حقيقي بيعرفهم**. لو حد عمل refresh وهو واقف على `/admin` أو `/cashier` مباشرة، هياخد **404** من غير إعداد صحيح.

**الحل:** لازم تتأكد إن فيه rewrite rule بيوجه كل الطلبات لـ `index.html`:

- **لو Vercel**: هيعمل ده تلقائيًا لمشاريع Vite في أغلب الأحيان، لكن للتأكيد ضيف ملف `vercel.json` في جذر المشروع:
```json
{
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
```

- **لو Netlify**: ضيف ملف `public/_redirects` بالسطر ده:
```
/*    /index.html   200
```

**لازم تتأكد من النقطة دي بعد النشر مباشرة** — افتح `/admin` مباشرة من المتصفح (مش بالنقر من `/`) وشوف هل بيفتح صح ولا 404.

### 5. متغيرات البيئة (Environment Variables)
مفيش حاجة إلزامية — مفاتيح Supabase (URL + anon key) مكتوبة مباشرة في `src/supabase.js` حاليًا (وده مقبول لأن الـ anon key مصمم يكون عام زي أي public client config، مش سر).

**تحسين اختياري (مش ضروري للنشر يشتغل):** ينفع تتنقل لـ environment variables (`VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`) بدل ما تكون مكتوبة جوه الكود، عشان تقدر تغيّرهم من غير ما تعدل كود، لو حبيت مستقبلًا. مش أولوية دلوقتي.

### 6. بعد النشر — قائمة تأكيد
- [ ] `/` بتفتح وتعرض المنيو صح
- [ ] `/admin` بترجع صفحة تسجيل الدخول (مش 404) حتى لو فتحتها مباشرة
- [ ] `/cashier` بترجع صفحة الدخول (مش 404) حتى لو فتحتها مباشرة
- [ ] تسجيل الدخول لـ `/admin` بحساب Supabase Auth شغال
- [ ] عمل أوردر تجريبي من `/?table=1` ومتابعته
- [ ] لو عندك دومين خاص، اربطه من إعدادات Vercel/Netlify (Custom Domain)

## بعد ما ينشر بنجاح
الخطوة الجاية (في جلسة تانية): توحيد نظامي "المدير" المذكورين في `CLAUDE.md`.
